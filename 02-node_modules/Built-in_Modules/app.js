/**
  fs      → File System
  path    → File paths
  os      → Operating system
  http    → HTTP server
  events  → Events
  crypto  → Cryptography
*/

const os = require("os");

console.log(os.platform());
console.log(os.arch());
console.log(os.cpus());
